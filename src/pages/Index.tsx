const Index = ({ children }: { children: JSX.Element }) => {
  return (
    <>
      <main className='py-2 h-full flex justify-center items-center'>{children}</main>
    </>
  )
}

export default Index
